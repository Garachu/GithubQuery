<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        Ask Android to show a button on the action bar
        If there isn't room to show the Button on the action bar it will still appear on the overflow menu
        use "app:showAsAction" rather than "android:showAsAction to keep things compatible with GingerBread and the support library"
    -->

    <item
        android:id="@+id/action_search"
        android:orderInCategory="1"
        android:title="@string/search_menu"
        app:showAsAction="ifRoom"/>
</menu>